# NextAuth (now called Auth.js) is used in a Next.js app to handle login & user authentication easily — without writing complex backend code yourself.

# note : <Link href={"/login"}> 
#        At this step app/login/page.js will executed instead of /app/page.js
#        And Navbar and Footer etc will remain same

# we created api/auth/[...nextauth]/route.js instead of api/auth/[...nextauth].js
#          because it api end point


# [username]/page.js :-> dynamic routes